//import { io } from 'socket.io-client';

//// "undefined" significa que va a usar el objeto `window.location`
//const URL:string = 'http://localhost:4000';

//export const socket = io(URL, {
//    autoConnect: false
//});